/**
 * Public URL prefix for the retro computer's assets.
 *
 * The original Vite project read this from `import.meta.env.BASE_URL` (set to
 * `/portfolio/` by its vite.config). Under Next the assets are served straight
 * out of `public/retro/`, so it is a constant.
 */
export const BASE_PATH = "/retro"
